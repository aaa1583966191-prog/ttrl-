# AutoDL Claude Code 快速配置指南

## 🚀 一键安装（复制粘贴即可）

```bash
# 安装 NVM 和 Node.js
conda remove nodejs -y
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.0/install.sh | bash
export NVM_DIR="$HOME/.nvm" && [ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
nvm install 20 && nvm use 20

# 配置环境变量（注意替换你的 API Key）
cat >> ~/.bashrc << 'EOF'

export ANTHROPIC_AUTH_TOKEN="你的API_KEY"
export ANTHROPIC_BASE_URL="https://api.ikuncode.cc"
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
EOF

source ~/.bashrc

# 启动 Claude Code
cd /root/autodl-tmp && npx @anthropic-ai/claude-code
```

---

## 📋 快速启动脚本（推荐）

创建一次，以后直接用：

```bash
cat > ~/start-claude.sh << 'EOF'
#!/bin/bash
export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"
export ANTHROPIC_AUTH_TOKEN="你的API_KEY"
export ANTHROPIC_BASE_URL="https://api.ikuncode.cc"
cd /root/autodl-tmp
npx @anthropic-ai/claude-code
EOF

chmod +x ~/start-claude.sh
```

以后每次启动只需：
```bash
~/start-claude.sh
```

---

## 🔧 常见问题速查

| 问题 | 解决方案 |
|------|---------|
| `symbol lookup error` | `conda remove nodejs -y` 然后用 NVM 安装 |
| `command not found` | 使用 `npx @anthropic-ai/claude-code` |
| 连接到 `api.anthropic.com` | `source ~/.bashrc` 重新加载环境变量 |
| heredoc 卡住（很多 `>`） | 按 `Ctrl+C` 或输入 `EOF` |

---

## ✅ 验证配置

```bash
node -v          # v20.x.x
npm -v           # 10.x.x
echo $ANTHROPIC_AUTH_TOKEN  # sk-xxx...
echo $ANTHROPIC_BASE_URL    # https://api.ikuncode.cc
```

---

## 💡 虚拟环境使用

完全支持！环境变量设置后，在任何虚拟环境中都可以使用：

```bash
conda activate your_env
~/start-claude.sh  # 或 npx @anthropic-ai/claude-code
```

---

**获取 API Key**: https://docs.ikuncode.cc/
